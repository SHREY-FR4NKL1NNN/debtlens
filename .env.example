# Frontend environment (Vite). Copy to .env.local to override.

# DebtLens backend (FastAPI). Defaults to the local dev port.
VITE_API_URL=http://localhost:8010

# LocalMind AI router for streaming explanations.
VITE_LOCALMIND_URL=http://localhost:8000
